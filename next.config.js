/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['st.depositphotos.com', 'farm2.staticflickr.com', 'bipbap.ru', '127.0.0.1', 't4.ftcdn.net'],
  },
};

module.exports = nextConfig;
