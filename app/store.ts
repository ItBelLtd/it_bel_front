import { create } from 'zustand';

// type Store = {
//   count: number;
//   isActive: boolean;
//   inc: () => void;
// };

// export const useStore = create<Store>()((set) => ({
//   count: 1,
//   isActive: true,
//   inc: () => set((state) => ({ count: state.count + 1 })),
// }));
